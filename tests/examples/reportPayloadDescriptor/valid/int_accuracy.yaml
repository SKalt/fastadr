# yaml-language-server: $schema=../../../../spec/1_oadr3.0.1.yaml#/components/schemas/reportPayloadDescriptor
{
  "payloadType": " ",
  "accuracy": 0, # <- must be a float, but all ints are also floats
  # see https://docs.pydantic.dev/latest/concepts/conversion_table/
}
